ALTER TABLE user_requests
    ADD COLUMN IF NOT EXISTS request_payload TEXT,
    ADD COLUMN IF NOT EXISTS total_price NUMERIC(12,2),
    ADD COLUMN IF NOT EXISTS minimal_price NUMERIC(12,2),
    ADD COLUMN IF NOT EXISTS currency_code VARCHAR(3),
    ADD COLUMN IF NOT EXISTS response_payload TEXT,
    ADD COLUMN IF NOT EXISTS status VARCHAR(16) NOT NULL DEFAULT 'SUCCESS',
    ADD COLUMN IF NOT EXISTS error_message TEXT;
